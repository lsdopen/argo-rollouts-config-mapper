{{- include "argo-rollouts-config-mapper.validateValues" . -}}
{{- if eq .Values.certificates.method "cert-manager" }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "argo-rollouts-config-mapper.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "argo-rollouts-config-mapper.labels" . | nindent 4 }}
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  secretName: {{ include "argo-rollouts-config-mapper.certificateSecretName" . }}
  {{- with .Values.certificates.certManager.duration }}
  duration: {{ . }}
  {{- end }}
  {{- with .Values.certificates.certManager.renewBefore }}
  renewBefore: {{ . }}
  {{- end }}
  subject:
    organizationUnits:
      - {{ .Release.Namespace }}
  commonName: {{ include "argo-rollouts-config-mapper.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  dnsNames:
    - {{ include "argo-rollouts-config-mapper.serviceName" . }}
    - {{ include "argo-rollouts-config-mapper.serviceName" . }}.{{ .Release.Namespace }}
    - {{ include "argo-rollouts-config-mapper.serviceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "argo-rollouts-config-mapper.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: {{ .Values.certificates.certManager.issuer.name }}
    kind: {{ .Values.certificates.certManager.issuer.kind }}
    {{- if eq .Values.certificates.certManager.issuer.kind "Issuer" }}
    group: cert-manager.io
    {{- end }}
{{- end }}
